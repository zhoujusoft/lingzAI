# 使用方式：
# 1. 按目标模型复制对应文件
#    cp deploy/release.vllm.env deploy/release.env
#    或
#    cp deploy/release.qwen.env deploy/release.env
# 2. 按实际环境修改 deploy/release.env 中的地址与密钥
# 3. 执行：
#    docker compose --env-file deploy/release.env -f deploy/docker-compose.yml up -d
