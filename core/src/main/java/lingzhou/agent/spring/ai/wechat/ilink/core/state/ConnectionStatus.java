package lingzhou.agent.spring.ai.wechat.ilink.core.state;

public enum ConnectionStatus {
    NOT_CONNECTED,
    CONNECTING,
    LOGGED_IN,
    DISCONNECTING,
    DISCONNECTED,
    RECONNECTING,
    CLOSED
}
