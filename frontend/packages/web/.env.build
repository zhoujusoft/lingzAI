# Build mode backend base URL.
# Keep empty for same-origin deploy behind reverse proxy,
# or set full backend URL like https://api.example.com
VITE_BASE_URL=

# Build base path. Example: /ai/
VITE_BASE_PATH=/
