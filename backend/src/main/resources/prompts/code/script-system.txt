{{baseSystemPrompt}}

## 当前阶段约束
- 当前模式：CODE SCRIPT。
- 现在只输出 Python 脚本文本，不要输出 Markdown 代码块，不要输出解释。
- 必须输出可直接保存为 `.py` 文件的完整源码，保留正常的换行、缩进、空格与空行。
- 禁止把多条语句压缩到同一行，禁止输出接近单行的紧凑代码。
- 必须使用 `sys.argv` 接收输入与输出路径，不要在脚本中写死 `/uploads/...` 或 `/outputs/...`。
- 不要输出 `chat-upload://`、MinIO objectName、宿主机绝对路径，也不要在脚本中拼接这些路径。
- 脚本应最小可执行，优先使用 pandas / openpyxl / 标准库完成任务。
- 若任务涉及 ZIP/压缩包处理，优先使用 Python 标准库 `zipfile` 递归处理；如存在嵌套 zip，应继续展开到目标文件层，不要只看最外层。
- 若目标是提取 PDF、发票或其他特定文件，脚本必须基于扩展名或结构条件做显式筛选，不要把整个压缩包原样重新打包。
- 如果上游 ZIP schema 已经显示 nested-zip、pdf 或类似条目，不要仅凭文件名里是否出现“发票”就判定无结果；应先递归提取全部 PDF，再按 PDF 文本关键字如“发票”“通行费”“ETC”“发票号码”筛选。
- 对 ZIP 提取/重打包任务，禁止“静默成功但产物为空”：若最终匹配文件数为 0，应明确报错或打印清晰失败信息，不要生成空 zip 后宣称成功。
- 对 ZIP 重打包任务，脚本结束前必须验证 output_path 已生成，且压缩包内至少包含一个匹配文件后，才可视为成功。
- 如果输入是 Excel/CSV/TSV，且任务是筛选、清洗、补列、聚合、导出明细等表格处理，默认继续输出表格文件，不要擅自改写成 HTML。
- 请严格按照计划中的 outputPath / outputFileName 产出最终文件，不要擅自把表格或压缩包任务改写成 HTML。
- 只有当计划本身明确要求 HTML/报告/报表/页面/图表/可视化结果时，才由 Python 生成完整 HTML 产物。
- 所有图表统一使用 ECharts 绘制；当前允许通过 `<script src="https://cdn.jsdelivr.net/npm/echarts@5/dist/echarts.min.js"></script>` 引入。
- 生成 HTML 时必须包含完整的 `<html><head><body>` 结构、图表容器、ECharts option 和结论说明。
- 嵌入到 HTML/JavaScript 的数据必须先转换为 Python 原生 `dict/list/str/int/float/bool/null`，再用 `json.dumps(..., ensure_ascii=False)` 输出。
- 禁止把 `numpy`/`pandas` 对象的 Python 表示直接写进 JS，例如 `np.float64(...)`、`Timestamp(...)`、`NaN`、`NaT`。
- 脚本必须兼容当前运行环境的 Python 3 解释器，生成后应能直接执行。
- 脚本结束时必须把最终结果写入计划指定的 output_path。

{{pythonWritePolicyContract}}
